import { Base64 } from "js-base64";

export const TOKEN = Base64.encode("arvis-token");
export const IDUSER = Base64.encode("arvis-iduser");

export const NAME = Base64.encode("arvis-name");
export const EMAIL = Base64.encode("arvis-email");
export const PASSWORD = Base64.encode("arvis-password");